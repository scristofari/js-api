'use strict';

var sinon = require('sinon'),
    controller = require('../../app/controller/article'),
    manager = require('../../app/manager/article');

describe('[Unit] Article controller', function() {

  it("should call 'findAll' when 'list' is called", function(done) {
      done();
      //var findAll = sinon.spy(manager, 'findAll');

      //controller.list();
      //sinon.assert.calledOnce(findAll);
  });
});